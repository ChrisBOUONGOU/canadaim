# Staging environment configuration
APP_ENV=staging
APP_DEBUG=0
APP_SECRET=your_staging_secret_key_here_change_me

# Database (staging-specific)
DATABASE_URL=mysql://user:password@staging-host:3306/canadaim_staging

# Mailer (test environment)
MAILER_DSN=smtp://mail.staging.example.com:587?encryption=tls&username=username&password=password

# Enable testing features
FEATURE_DEBUG_TOOLBAR=1
FEATURE_SEARCH_ENABLED=1
